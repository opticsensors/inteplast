# Correcciones de la revisión del 2026-09-15

Revisión de código, documentación y operación del piloto INTEPLAST. Se corrigieron los fallos
detectados sin copiar ni hidratar los ficheros del cliente. No hay cambios en las tablas de la BD;
sí cambia el contrato de acceso a ficheros y se ha regenerado el cliente TypeScript.

## Aplicación y operación

| Fallo | Comportamiento corregido |
|---|---|
| Subidas fuera del volumen persistente | El Dockerfile fija `UPLOADS_DIR=/app/uploads`, igual que el montaje de `app-uploads` |
| Tests con la BD/volúmenes de trabajo | `compose.test.yml` independiente, proyecto por ejecución, BD `app_test` y almacenamiento temporal; pytest rechaza nombres ajenos a `_test` |
| Argumentos de pytest ignorados | Los lanzadores PowerShell/Bash y el script de cobertura los pasan a pytest |
| Dockerfiles pedían un `bun.lock` inexistente | Instalación con npm y el `package-lock.json` versionado; contexto Docker excluye dependencias locales y salidas generadas |
| Refrescar notas borraba la cabecera en edición | Los campos modificados y la imagen pendiente se conservan al refrescar; solo se envían los campos de cabecera editados |
| Plegar/salir descartaba el autoguardado pendiente | Las secciones conservan sus editores; se completan guardados y subidas antes de salir; los fallos conservan el borrador |
| Un refresco sobrescribía cambios ajenos | El autoguardado escribe solo cambios locales y serializa las peticiones de cada fila |
| Registro público daba acceso al conocimiento | Desactivado por defecto; Admin crea las cuentas; rutas de aprovisionamiento exclusivas del stack de tests |
| Conocer un UUID permitía descargar ficheros | Bearer o enlace firmado de 15 minutos, limitado al fichero y a un usuario activo; un token de fichero no es un token de sesión |
| Contenido activo servido en línea | HTML/SVG y otros tipos no permitidos se descargan como adjuntos; `nosniff`, política de referencia y caché privada |
| Se ofrecía vista previa para tipos que se descargan | La selección y el arrastre de imágenes de cabecera admiten solo los formatos visibles; el visor de adjuntos aplica el mismo contrato de tipos que la API |
| `null` o referencias inexistentes terminaban en error de BD | 422 para `null` en campos no anulables; 404 para referencias de fichero inexistentes; los campos anulables siguen admitiendo `null` |
| El total de piezas era solo el tamaño de página | `count` representa todas las coincidencias antes de paginar |
| Un fallo del seed dejaba medio ejemplo | Una transacción para toda la carga, con rollback y reintento; el ejemplo existente no se modifica |
| Los tests de login esperaban texto eliminado | Selectores del dashboard actual y aprovisionamiento de usuarios adaptado al registro cerrado |
| El test de correo no abandonaba el campo | `blur()` explícito para comprobar la validación, sin depender de dónde cae un clic sobre `body` |

La base continúa siendo colaborativa para los usuarios dados de alta. Los permisos de edición
y borrado están descritos en [app-web.md](app-web.md). El registro cerrado no crea roles nuevos.
Los puertos del override de desarrollo se limitan a `127.0.0.1`.

**Actualizar instalaciones anteriores:** antes de recrear el backend, rescatar posibles bytes de
`/app/backend/uploads`. La corrección no reconstruye ficheros ya perdidos. Ver
[deployment.md](../deployment.md). El seed tampoco sustituye una copia de seguridad.

## Visores

- Corregir el signo conocido de B2/B4 en `Posición Z` recalcula desviación, exceso y NOK. Se
  conservan los campos originales del export y aplicar la corrección dos veces no invierte otra vez.
- Un PDF con campos de medida ausentes o no finitos queda **desconocido**, con explicación y
  huecos en las gráficas. No se convierte en una medición dentro de tolerancia.
- Rutas de salida coherentes, `--solo-inicio` en una carpeta nueva, validación de argumentos
  numéricos y caché OCR ligada al contenido de origen.

Ver [visores.md](visores.md) y [data-explorer/README.md](../data-explorer/README.md).

## Documentación del dominio

- Se reabre la pregunta sobre retoques posteriores. No observar variaciones grandes no prueba
  que no hubiera una tercera corrección: hay acciones documentadas de 0,02 y 0,03 mm.
- Los CSV de cavidad son la fuente de medidas; `HISTORY` aporta metadatos. `totes.csv` es
  redundante y no se prioriza como una ingesta independiente.
- Se distingue la selección de cotas en los XLS de la cobertura de sus CSV. Se corrigen rutas
  de scripts, referencias de celdas, recuentos y afirmaciones no demostradas sobre formatos.
- Se reescriben los párrafos truncados de `TODO.md`, manteniendo sus propuestas como propuestas.
- Las guías de desarrollo y despliegue reflejan npm, las rutas reales, la edición en línea y los
  tests aislados. Se eliminan promesas de CI/precommit que no existen en este repo.

**Pendiente de evidencia del cliente:** confirmar los retoques posteriores/cierre del molde,
el signo de tolerancia de N266 y el recuento contradictorio de imágenes del método de medida.
No se han resuelto contradicciones inventando valores. Ver [preguntas-abiertas.md](preguntas-abiertas.md).

## Verificación

- Suite del backend: **101 pruebas**, ejecutadas en PostgreSQL temporal; **92 %** de cobertura
  en la ejecución del lanzador PowerShell. Ruff y mypy pasan.
- La ejecución de pytest con `POSTGRES_DB=app` se rechaza antes de recoger pruebas, comprobado
  contra el contenedor de tests, sin conectar a la BD de trabajo.
- Persistencia: escribir un fichero en el volumen de pruebas, eliminar el contenedor y leerlo
  desde otro contenedor conserva su contenido. El volumen de comprobación se retira al terminar.
- La instalación local se reconstruye y queda saludable con `/app/uploads` como destino.
  Los recuentos de features, ficheros y usuarios se conservan tras la actualización.
- Visores: **7 pruebas sintéticas** sin leer datos del cliente.
- Componentes: **15 regresiones** con los editores reales, API simulada y peticiones externas
  bloqueadas. Cubren pérdidas de borrador, concurrencia, fallos, navegación y subidas. Cerrar sesión
  espera el guardado y cancelar la salida conserva credenciales y borradores. Se comprueba además
  el rechazo de imágenes no admitidas al seleccionar/arrastrar y la decisión de vista previa por MIME.
- Navegador: **52 pruebas E2E** pasan en el stack temporal, incluidos administración, acceso,
  recuperación de contraseña, perfil y cierre del registro público.
- PDF sintético: Chromium 145.0.7632.6 muestra el visor nativo con la cabecera CSP `sandbox`,
  tanto en un iframe del mismo origen como entre orígenes distintos. Se conserva esa protección.
- El build del frontend pasa tras un `npm ci` limpio en Docker. Las advertencias de tamaño de
  chunks y de módulos Node opcionales de OCCT permanecen; no impiden generar el paquete.
- Los ficheros Compose y los lanzadores Bash/PowerShell pasan sus comprobaciones de sintaxis.
- Biome pasa; se excluyen del análisis el OpenAPI intermedio y los resultados generados por
  Playwright, incluida su sesión de autenticación temporal.

## Dependencias

Se actualizan Axios, form-data, Vite y las dependencias transitivas señaladas por npm. Hay
overrides acotados para Handlebars, tar (dentro de giget) y esbuild. El lockfile se instala
desde cero en los Dockerfiles, sin reutilizar `node_modules` de la máquina.

`npm audit --omit=dev` informa **0 vulnerabilidades**. La auditoría completa conserva **1 aviso
moderado** del generador `@hey-api/openapi-ts` 0.73.0:
[GHSA-hhx9-57xq-r5rw](https://github.com/hey-api/hey-api/security/advisories/GHSA-hhx9-57xq-r5rw).
El aviso afecta a la plantilla `buildClientParams`/`params.gen.ts`; el cliente `legacy/axios`
generado aquí no contiene ese fichero ni esa función. No se ha forzado la migración incompatible
del generador, y no se presenta la auditoría completa como limpia.
